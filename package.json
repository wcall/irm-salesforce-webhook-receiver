{
  "name": "irm-salesforce-webhook",
  "version": "1.0.0",
  "description": "Express receiver that turns Grafana IRM webhooks into Salesforce Cases.",
  "type": "module",
  "main": "receiver.js",
  "scripts": {
    "start": "node --env-file=.env receiver.js",
    "dev": "node --env-file=.env --watch receiver.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2",
    "node-fetch": "^3.3.2"
  }
}
